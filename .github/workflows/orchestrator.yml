name: Orchestrator

on:
  push:
    branches: [ main, master, devs ]
  pull_request:
    branches: [ main, master ]

jobs:
  call_web_app_workflow:
    uses: ./../web-app/.github/workflows/playwright.yml@main
    with:
      working-directory: './../web-app'

