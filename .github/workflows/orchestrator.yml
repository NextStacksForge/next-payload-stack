name: Orchestrator

on:
  push:
    branches: [ main, master, devs ]
  pull_request:
    branches: [ main, master ]

jobs:
  web_app_tests:
    uses: ./web-app/.github/workflows/playwright.yml
    with:
      working-directory: './web-app'